# Server Configuration
PORT=87

# Kleinanzeigen Password (for automated login)
KLEINANZEIGEN_PASSWORD=

# Chrome WebSocket Endpoint (auto-detected if not set)
# CHROME_WS_ENDPOINT=ws://localhost:9222/devtools/browser/...

# Debug Mode
DEBUG=false
LOG_LEVEL=info

# ============================================
# OAUTH2 CONFIGURATION (for email verification)
# ============================================

# Microsoft / Outlook / Hotmail
# Steps to get these values:
# 1. Go to https://portal.azure.com
# 2. Azure Active Directory > App registrations > New registration
# 3. Name: "Kleinanzeigen API"
# 4. Redirect URI: http://localhost:87/oauth/microsoft/callback
# 5. Copy Application (client) ID -> MICROSOFT_CLIENT_ID
# 6. Certificates & secrets > New client secret -> MICROSOFT_CLIENT_SECRET
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_TENANT_ID=common
MICROSOFT_REDIRECT_URI=http://localhost:87/oauth/microsoft/callback

# Gmail / Google
# Steps to get these values:
# 1. Go to https://console.cloud.google.com
# 2. Create new project or select existing
# 3. APIs & Services > Enable Gmail API
# 4. APIs & Services > Credentials > Create OAuth Client ID
# 5. Application type: Web application
# 6. Authorized redirect URIs: http://localhost:87/oauth/gmail/callback
# 7. Copy Client ID -> GMAIL_CLIENT_ID
# 8. Copy Client Secret -> GMAIL_CLIENT_SECRET
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REDIRECT_URI=http://localhost:87/oauth/gmail/callback
